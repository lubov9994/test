double countAvg( double[] arr ) {
	int count = arr.length;
	double sum = 0;
	
	if ( count != 0 ) {

		for( int i = 0; i < count; i++ ){
			sum += arr[ i ];
		}
	
		return sum / count;
	}

	return 0;
}